# HIERARCHY.md — Иерархия источников правды

Этот файл фиксирует, чьё слово главнее при конфликте правил. Читать первым.

---

## Пирамида приоритетов

```
Уровень 1 (абсолютный) — Эталоны стиля
  1expert_style_example.md.txt
  1client_style_example.md.txt
        ↓
Уровень 2 — Личность и стиль v2
  v2/IDENTITY.md
  v2/STYLE_GUIDE.md
        ↓
Уровень 3 — Протоколы v2
  v2/KNOWLEDGE_PROTOCOL.md
  v2/PIPELINES.md
  v2/SELF_LEARNING.md
        ↓
Уровень 4 — Общие правила
  RULES.md
        ↓
Уровень 5 — Специализированная логика шагов
  skills/*.md
        ↓
Уровень 6 — База знаний (выжимки, приоритет при цитировании)
  references/*.md
        ↓
Уровень 7 — Первичный материал (когда в references нет ответа)
  text_extracted/*.txt
```

---

## Правило конфликта

При конфликте между уровнями — побеждает **верхний**. Всегда.

Примеры:
- `v2/STYLE_GUIDE.md` требует 6 обязательных слотов для нутрицевтика, `RULES.md` требует 25–35 строк → побеждает `v2/STYLE_GUIDE.md`.
- Эталон показывает 7 видимых шагов мышления, `skills/analyze_blood.md` даёт линейную последовательность → побеждает эталон.
- `v2/KNOWLEDGE_PROTOCOL.md` говорит «пометь флагом [⚠ требует верификации]», `RULES.md` говорит «[Источник: общие знания]» → побеждает `v2/KNOWLEDGE_PROTOCOL.md`.

Если конфликт замечен агентом во время работы — он записывает это в `learning/corrections.md` с пометкой, какой уровень победил и почему.

---

## Правило применения v2

Всё, что написано в `v2/`, заменяет соответствующие правила в `RULES.md` и `skills/*.md`. `RULES.md` действует только в тех областях, которые `v2/` не покрывает.

Агент не молчит при конфликте — он делает выбор в пользу верхнего уровня и документирует его.

---

## Эталоны — неприкосновенны

Файлы `1expert_style_example.md.txt` и `1client_style_example.md.txt` — это реальные образцы хороших ответов. Их нельзя редактировать, нельзя «оптимизировать», нельзя заменить. Их можно только читать и сверяться.

---

## Команда `/consult`: режимы strict и fallback

Два явных режима поведения при неполных данных клиента:

**`strict`** (по умолчанию) — не запускать анализ, если отсутствуют обязательные данные (пол, возраст, рост, вес, жалобы, лекарства). Сначала запросить недостающее.

**`fallback`** — работать с тем что есть, явно отмечая каждый пробел. Активируется только при явной команде пользователя «работай с тем что есть» или при передаче данных inline в команде.

В режиме `fallback`:
- КБЖУ считается только при наличии роста и веса, иначе — только ориентировочные значения с пометкой «⚠ приблизительно, данных недостаточно».
- Каждый вывод без данных помечается: «⚠ данных нет — вывод основан на предположении».

---

## Что читает агент перед каждым пайплайном

1. Этот файл (`v2/HIERARCHY.md`) — уровни и правила конфликтов.
2. `learning/corrections.md` — накопленные исправления ошибок.
3. `learning/patterns.md` — выявленные паттерны.
4. Профиль клиента (`clients/[Фамилия]/profile.md`) и анализы.
5. Профильные файлы из `references/` по выявленным темам.


---

## Иерархия медицинских норм (референсы и оптимумы)

Этот раздел определяет, откуда агент берёт цифры при оценке анализов.

**Два класса значений:**
1. **Референс** — мировой доказательный стандарт. Источник: гайдлайны, лабораторные стандарты, крупные исследования. НЕ позиция школы.
2. **Оптимум** — целевое значение школы (экспертная позиция). Помечается [школа]. Это НЕ доказанный порог, а цель.

**Приоритет при оценке:**
- Есть болезнь или нет — определяется по РЕФЕРЕНСУ (мировой стандарт).
- Достаточно ли хорошо (цель) — определяется по ОПТИМУМУ школы.
- В выводе агент разделяет: "в референсе / вне референса" и "достигнут оптимум / ниже оптимума".

**Где какая цифра лежит:**
- Актуальные (рабочие) референсы и оптимумы — ТОЛЬКО в `references/11_reference_values.md`. Именно их агент использует при оценке.
- История эволюции оптимумов — в `references/optimums_changelog.md` (архив: было -> стало -> дата -> причина -> источник).

**Правило синхронизации:**
- При оценке агент берёт текущий оптимум из ТАБЛИЦЫ `11_reference_values.md`.
- Строка "Текущий оптимум школы" в журнале ОБЯЗАНА совпадать с таблицей.
- Если значения в таблице и журнале расходятся — это ОШИБКА синхронизации. Агент не гадает, а сообщает о расхождении.
- При изменении оптимума правится ДВА места: новое значение в таблице + новая строка в журнал (было -> стало -> дата -> причина -> источник).

**Правило метода:**
- Для каждого показателя указывается метод измерения.
- Референс метод-зависимых показателей (ферритин, гормоны, витамины группы B) может отличаться по прибору — берётся по методу из бланка.
- Стандартизованные показатели (липиды, HbA1c, глюкоза) — референс единый, метод указывается справочно.